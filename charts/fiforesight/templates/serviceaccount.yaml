apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "fiforesight.serviceAccountName" . }}
  labels:
    {{- include "fiforesight.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
