fastapi
uvicorn
httpx
influxdb-client
python-dotenv
pandas
numpy
statsmodels
prophet
scikit-learn
# scipy.signal.find_peaks powers RSI/MACD divergence detection (Feature 14).
# Already a transitive dep of prophet/scikit-learn — declared explicitly here
# (unpinned, matching the rest of this manifest).
scipy
yfinance
newrelic
# Sentiment analysis
vaderSentiment
# LangGraph jury orchestration
langgraph
langchain-core
# MCP server
fastmcp
# Native Redis client with hiredis C extension for faster response parsing
redis[hiredis]
# JWT verification for Supabase auth tokens. [crypto] pulls in `cryptography`,
# required to verify asymmetric ES256/RS256 tokens via the project JWKS.
PyJWT[crypto]
# Per-IP / per-user rate limiting
slowapi
# Web Push (VAPID) delivery for Alerts & Notifications (Feature 9). Pulls in
# py-vapid + http-ece for payload encryption. Free, no third-party service.
pywebpush